# 3. Can we have a set with 18(int) and  '18'(str) as a value in it? If yes, then write a code to create such a set.

# Yes, we can have a set with both 18 (int) and '18' (str) because they are different data types.
mixed_set = {18, '18'}
print(mixed_set)